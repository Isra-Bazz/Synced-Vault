wallbash info is kinda stored [here](https://github.com/HyDE-Project/HyDE/tree/master/Configs/.config/hyde/wallbash)
config.tmol is another story 
config.tmol is a one use file that copies the schema of the HyDE repo to set initially as the schema until its changed by .local/share/hyde/config.toml and then its unused
